// src/filters.rs

use pyo3::prelude::*;

#[pyfunction]
pub fn search_filter(query: &str) -> Vec<String> {
    panic!("search_filter is not yet implemented. This function must be implemented before use.");
}

#[pyfunction]
pub fn search_possible_filter() -> Vec<String> {
    panic!("search_possible_filter is not yet implemented. This function must be implemented before use.");
}
